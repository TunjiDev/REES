/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
  images: {
    domains: ["lldfdtyvpeitatttmmnd.supabase.co"],
  },
};

module.exports = nextConfig;
